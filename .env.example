# API settings (consumed by core.settings.ApiSettings, prefix API_)
API_KEY=changeme
API_SECRET=changeme

# Postgres settings (consumed by core.settings.DatabaseSettings
# and by the db service in docker/docker-compose.yml)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=cookbook
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
